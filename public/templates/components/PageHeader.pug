mixin PageHeader()
	template
		header.host.page-header&attributes(attributes)
			transition-box(
				appear
				appear-active-class="v-appear-active"
				:duration="600"
				@after-appear="startAnimateIsEnd = true"
			)
				.logo-box
					a.logo(
						href="/"
						title="Zingy Projects"
					)
						base-icon.logo-icon(icon-name="zingy")

			transition-box(
				:duration="{enter: 500, leave: 300}"
				@enter="onLNavBoxEnter"
				@after-enter="onLNavBoxAfterEnter"
				@before-leave="onLNavBoxBeforeLeave"
			)
				.nav-box(v-show="startAnimateIsEnd && !headerIsMinimized")
					page-header-nav-is-top.page-header-nav-is-top.nav

			transition-box(:duration="{enter: 550, leave: 350}")
				.menu-button-box(v-show="startAnimateIsEnd && headerIsMinimized")
					button.menu-button(
						type="button"
						@click="sidebarIsOpened = !sidebarIsOpened"
					)
						base-icon.menu-button-icon(icon-name="menu")

			transition-box(
				:duration="{enter: 500, leave: 300}"
				@enter="onLRightPanelBoxEnter"
				@after-enter="onLRightPanelBoxAfterEnter"
				@before-leave="onLRightPanelBoxBeforeLeave"
			)
				.right-panel-box(v-show="startAnimateIsEnd && !headerIsMinimized")
					.right-panel
						.right-panel-grid
							.right-panel-grid-cell
								a(href="").link
									base-icon.link-icon(icon-name="globe")
									.link-text.is-uppercase Eng

							.right-panel-grid-cell
								a(href="tel:+79854575817").link
									base-icon.link-icon(icon-name="phone")
									.link-text +7 985 457 58 17

							.right-panel-grid-cell
								base-button.button(
									tag="a"
									href=""
								) Начать проект

			transition-box(:duration="{enter: 500, leave: 300}")
				.phone-button-box(v-show="startAnimateIsEnd && headerIsMinimized")
					a.phone-button(href="tel:+79854575817")
						base-icon.phone-button-icon(icon-name="phone")

			transition-group.sidebar-box(
				tag="div"
				:duration="{enter: 600, leave: 600}"
			)
				.sidebar-backdrop(
					v-if="sidebarIsOpened"
					@click="sidebarIsOpened = false"
					key="1"
				)

				.sidebar(
					v-if="sidebarIsOpened"
					key="2"
				)
					.sidebar-lang-box
						a(href="").link
							base-icon.link-icon(icon-name="globe")
							.link-text.is-uppercase Eng

					.sidebar-scrollable
						page-header-nav-is-aside.page-header-nav-is-aside.sidebar-nav

						.sidebar-footer
							.sidebar-footer-grid
								.sidebar-footer-grid-cell
									base-button.is-full-width.button(
										tag="a"
										href=""
									) Начать проект

								.sidebar-footer-grid-cell
									a(href="tel:+79854575817").link
										base-icon.link-icon(icon-name="phone")
										.link-text +7 985 457 58 17

								.sidebar-footer-grid-cell
									a(href="mailto:zingy.projects@gmail.com").link
										base-icon.link-icon(icon-name="envelope")
										.link-text zingy.projects@gmail.com


