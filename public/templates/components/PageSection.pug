mixin PageSection(title)
	template
		section.page-section&attributes(attributes)
			header.header
				.content-box
					.content-grid
						.content-grid-item
							h2.title= title

			.main
				block
