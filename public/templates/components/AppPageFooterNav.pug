mixin AppPageFooterNav(navList = [])
	template
		nav.app-page-footer-nav&attributes(attributes)
			block

			.l-grid
				each item in navList
					.l-grid-item
						a.nav-item(href="")
							span.nav-item-text= item
