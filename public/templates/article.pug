extends index

include components/SideNav
include components/AppArticle

append vars
	-
		title = 'Статья';

block app
	app-page(inline-template)
		+AppPage()
			a.link-backward(
				ref="linkBackward"
				href="blog.html"
			) Вернуться в блог

			a.link-up(
				ref="linkUp"
				href="#"
			)
				base-icon.link-up-icon(name="angle-left")

			.grid
				nav.app-page-breadcrumb(is="app-breadcrumb")
					a(href="index.html") Главная
					a(href="blog.html") Блог
					| Статья

				app-page-sticky-container.sticky-container(:options="stickyContainerOptions")
					template(v-slot:sidebar)
						aside
							side-nav.side-nav-wide.sticky-container-nav(
								inline-template
								v-if="windowWidth >= stickyContainerMinWidth ? true : true"
							)
								+SideNav()

							subscribe-form.sticky-container-subscribe(
								agree-url="blog.html"
								v-if="windowWidth >= 1024"
							)

					app-article.app-page-article(inline-template)
						+AppArticle()

				subscribe-form.subscribe-form-wide.app-page-subscribe-form(
					agree-url="blog.html"
					v-if="windowWidth < 1024"
				)

			feedback-section.app-page-feedback-section(inline-template)
				+FeedbackSection()
